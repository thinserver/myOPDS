<?php

# if method is GET:



# elseif method is POST: {
	$filename = $_POST['filename'];

	# open XML if exists
	# if not, make one and use default values

	# depending on which parameters have been provided
	# modify the XML

	#if (array_key_exists('title', $_POST)) {
	#	}
	}

?>
